import React from 'react';

const MyPage = () => {
    return (
        <input>
            <input type='button'>Change Theme</input>
        </>
    )
}

/**
 * On different pages, apply theme (dark vs light).
 * Button on top of pages called Change Theme, which switches.
 */
const useTheme = ({ theme }) => {

}